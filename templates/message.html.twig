{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="d-flex justify-content-center">
        <div class="box text-center" style="margin: 10em 0; padding: 1em">
            <p class="title">{{ title }}</p>
            <p>{{ message }}</p>
            <a class="btn" href="{{ path(redirect_app)~elem_ref }}">Clickez ici pour être redirigé</a>
        </div>
    </main>
{% endblock %}
